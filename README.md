# ThemeForest HTML Template

## Structure
- `HTML/` — template pages, assets and SCSS source
- `Documentation/` — buyer documentation
- `Licensing/` — third-party asset licenses

## Develop
```bash
npm install
npm run watch     # compile SCSS while editing
npm run build     # one-off build
npm run zip       # package for ThemeForest upload
```
